/*
function soma(v1, v2) {
    var resultado = v1 + v2
    return resultado
}
*/

/*
const soma = (v1,v2) => v1 + v2


console.log( soma(5,8) );
console.log( soma(2,7) );
console.log( soma(1,2) );

*/

/*
var valor = 10
while (valor > 0 ) {
    console.log(valor);
    valor = valor -1
}
*/

//  inicializador, condição, incrementador
for (let m = 0; m < 10; m = m + 2) {
    console.log(m);

}